---
slug: api
title: API
description: Pour les DÃ©veloppeurs
---

Le systÃ¨me Security Keypad fournit une API publique qui vous permet de vÃ©rifier les rÃ´les et permissions des joueurs depuis vos propres scripts Udon.

## Comprendre les Ã‰tiquettes de RÃ´le

Le systÃ¨me Security Keypad utilise deux types d'Ã©tiquettes de rÃ´le :

**RÃ´les Explicites** :
Ã‰tiquettes de rÃ´les auxquels le joueur s'est connectÃ© explicitement, soit en entrant un code, par autologin ou accordÃ© manuellement par n'importe quel systÃ¨me.

**RÃ´les Implicites :**
Ã‰tiquettes accordÃ©es parce que vous avez Ã©tÃ© explicitement assignÃ© Ã  l'un de leurs parents. Par exemple, si vous avez le rÃ´le VIP+ assignÃ© et que ce rÃ´le a VIP comme rÃ´le enfant, vous recevrez l'Ã©tiquette implicite VIP.

Ces 2 listes sont combinÃ©es en une seule liste **Roles** qui est synchronisÃ©e avec les utilisateurs distants. Cette division est utilisÃ©e en interne pour dÃ©terminer si vous devez vous dÃ©connecter d'un rÃ´le, et peut Ã©galement Ãªtre utilisÃ©e par des systÃ¨mes externes pour effectuer toutes sortes de vÃ©rifications selon vos besoins.

## Ã‰vÃ©nements Publics

Pour recevoir des Ã©vÃ©nements dans vos propres scripts Udon, vous devez d'abord ajouter votre UdonBehaviour Ã  la liste `Event Receivers` du SecurityKeypad. Vous pouvez le faire manuellement dans l'inspecteur ou avec les mÃ©thodes :

```csharp
RegisterListener(UdonBehaviour listener)
UnregisterListener(UdonBehaviour listener)
```

Une fois enregistrÃ©, votre script Udon peut implÃ©menter les mÃ©thodes d'Ã©vÃ©nement suivantes :

```csharp
- OnUserLogin(string playername, string rolename)
- OnUserLogout(string playername, string rolename)
- OnUserExplicitLogin(string playername, string rolename)
- OnUserExplicitLogout(string playername, string rolename)
```

## MÃ©thodes d'API Publique

### HasRole

La mÃ©thode `HasRole` vÃ©rifie si un joueur a un ou plusieurs rÃ´les. Cette mÃ©thode a plusieurs surcharges pour fournir de la flexibilitÃ© dans la faÃ§on dont vous vÃ©rifiez les rÃ´les.

**Retourne :**

- `true` si la condition du rÃ´le est remplie
- `false` sinon

**Exemples :**

```csharp
public SecurityKeypad keypad;
bool isAdmin = false;

void Start(){
    //VÃ©rifier si la rÃ©fÃ©rence du keypad est valide
    if(!Utilities.IsValid(keypad)) return;

    //VÃ©rifier si le joueur local est admin
    isAdmin = keypad.HasRole("Admin")
}
```

---

#### HasRole(KeyPadRol role)

VÃ©rifie si le joueur local s'est connectÃ© Ã  un rÃ´le spÃ©cifique.

**Signature :**

```csharp
public bool HasRole(KeyPadRol role)
```

**ParamÃ¨tres :**

- `role` - Le composant KeyPadRol Ã  vÃ©rifier

---

#### HasRole(KeyPadRol[] roles, bool ImplicitOnly, bool All)

VÃ©rifie si le joueur local a un ou plusieurs rÃ´les d'un tableau de composants KeyPadRol.

**Signature :**

```csharp
public bool HasRole(KeyPadRol[] roles, bool ImplicitOnly = false, bool All = false)
```

**ParamÃ¨tres :**

- `roles` - Tableau de composants KeyPadRol Ã  vÃ©rifier
- `ImplicitOnly` - Si `true`, vÃ©rifie uniquement les Ã©tiquettes de rÃ´le implicites (par dÃ©faut : `false`)
- `All` - Si `true`, nÃ©cessite que tous les rÃ´les correspondent ; si `false`, nÃ©cessite au moins un (par dÃ©faut : `false`)

---

#### HasRole(string role, bool ImplicitOnly)

VÃ©rifie si le joueur local a un rÃ´le par nom de rÃ´le.

**Signature :**

```csharp
public bool HasRole(string role, bool ImplicitOnly = false)
```

**ParamÃ¨tres :**

- `role` - Le nom du rÃ´le ou l'Ã©tiquette Ã  vÃ©rifier
- `ImplicitOnly` - Si `true`, vÃ©rifie uniquement les Ã©tiquettes de rÃ´le implicites (par dÃ©faut : `false`)

---

#### HasRole(VRCPlayerApi player, string role, bool ImplicitOnly)

VÃ©rifie si un joueur spÃ©cifique a un rÃ´le par nom de rÃ´le.

**Signature :**

```csharp
public bool HasRole(VRCPlayerApi player, string role, bool ImplicitOnly = false)
```

**ParamÃ¨tres :**

- `player` - Le joueur Ã  vÃ©rifier
- `role` - Le nom du rÃ´le ou l'Ã©tiquette Ã  vÃ©rifier
- `ImplicitOnly` - Si `true`, vÃ©rifie uniquement les Ã©tiquettes de rÃ´le implicites (par dÃ©faut : `false`)

---

#### HasRole(string[] roles, bool ImplicitOnly, bool All)

VÃ©rifie si le joueur local a un ou plusieurs rÃ´les d'un tableau de noms/Ã©tiquettes de rÃ´le.

**Signature :**

```csharp
public bool HasRole(string[] roles, bool ImplicitOnly = false, bool All = false)
```

**ParamÃ¨tres :**

- `roles` - Tableau de noms/Ã©tiquettes de rÃ´le Ã  vÃ©rifier
- `ImplicitOnly` - Si `true`, vÃ©rifie uniquement les Ã©tiquettes de rÃ´le implicites (par dÃ©faut : `false`)
- `All` - Si `true`, nÃ©cessite que tous les rÃ´les correspondent ; si `false`, nÃ©cessite au moins un (par dÃ©faut : `false`)

---

#### HasRole(VRCPlayerApi player, string[] roles, bool ImplicitOnly, bool All)

VÃ©rifie si un joueur spÃ©cifique a un ou plusieurs rÃ´les d'un tableau de noms/Ã©tiquettes de rÃ´le.

**Signature :**

```csharp
public bool HasRole(VRCPlayerApi player, string[] roles, bool ImplicitOnly = false, bool All = false)
```

**ParamÃ¨tres :**

- `player` - Le joueur Ã  vÃ©rifier
- `roles` - Tableau de noms/Ã©tiquettes de rÃ´le Ã  vÃ©rifier
- `ImplicitOnly` - Si `true`, vÃ©rifie uniquement les Ã©tiquettes de rÃ´le implicites (par dÃ©faut : `false`)
- `All` - Si `true`, nÃ©cessite que tous les rÃ´les correspondent ; si `false`, nÃ©cessite au moins un (par dÃ©faut : `false`)

---

### GetRoles

Les mÃ©thodes `GetRoles` rÃ©cupÃ¨rent les Ã©tiquettes de rÃ´le attribuÃ©es Ã  un joueur. Ces mÃ©thodes ont plusieurs surcharges pour diffÃ©rents cas d'usage.

**Retourne :**

- `DataList` contenant uniquement les Ã©tiquettes de rÃ´le explicites pour le joueur
- `DataList` vide si le systÃ¨me n'est pas initialisÃ© ou si aucune donnÃ©e de joueur n'est trouvÃ©e

---

#### GetRoles()

Obtient toutes les Ã©tiquettes de rÃ´le attribuÃ©es au joueur local.

**Signature :**

```csharp
public DataList GetRoles()
```

---

#### GetRoles(VRCPlayerApi player)

Obtient toutes les Ã©tiquettes de rÃ´le attribuÃ©es Ã  un joueur spÃ©cifique.

**Signature :**

```csharp
public DataList GetRoles(VRCPlayerApi player)
```

**ParamÃ¨tres :**

- `player` - Le joueur dont obtenir les rÃ´les

**Comportement :**

- Retourne une liste vide si appelÃ© avant l'initialisation (enregistre un avertissement)
- Retourne une liste vide si aucune donnÃ©e de joueur n'est trouvÃ©e

---

#### GetExplicitRoles()

Obtient uniquement les Ã©tiquettes de rÃ´le explicites attribuÃ©es au joueur local (rÃ´les auxquels ils se sont connectÃ©s).

**Signature :**

```csharp
public DataList GetExplicitRoles()
```

---

#### GetExplicitRoles(VRCPlayerApi player)

Obtient uniquement les Ã©tiquettes de rÃ´le explicites attribuÃ©es Ã  un joueur spÃ©cifique.

**Signature :**

```csharp
public DataList GetExplicitRoles(VRCPlayerApi player)
```

**ParamÃ¨tres :**

- `player` - Le joueur dont obtenir les rÃ´les explicites

**Comportement :**

- Retourne une liste vide si appelÃ© avant l'initialisation (enregistre un avertissement)
- Retourne une liste vide si aucune donnÃ©e de joueur n'est trouvÃ©e
- Inclut uniquement les rÃ´les auxquels le joueur s'est activement connectÃ©, pas les rÃ´les passifs/automatiques

---

#### GetImplicitRoles()

Obtient uniquement les Ã©tiquettes de rÃ´le implicites attribuÃ©es au joueur local (rÃ´les attribuÃ©s automatiquement).

**Signature :**

```csharp
public DataList GetImplicitRoles()
```

---

#### GetImplicitRoles(VRCPlayerApi player)

Obtient uniquement les Ã©tiquettes de rÃ´le implicites attribuÃ©es Ã  un joueur spÃ©cifique.

**Signature :**

```csharp
public DataList GetImplicitRoles(VRCPlayerApi player)
```

**ParamÃ¨tres :**

- `player` - Le joueur dont obtenir les rÃ´les implicites

**Comportement :**

- Retourne une liste vide si appelÃ© avant l'initialisation (enregistre un avertissement)
- Retourne une liste vide si aucune donnÃ©e de joueur n'est trouvÃ©e
- Inclut uniquement les rÃ´les attribuÃ©s automatiquement, pas les rÃ´les nÃ©cessitant une connexion explicite

---

### GetHighestRole

Les mÃ©thodes `GetHighestRole` rÃ©cupÃ¨rent le rÃ´le de plus haute prioritÃ© attribuÃ© Ã  un joueur en fonction de la hiÃ©rarchie des rÃ´les.

:::note
Le rÃ´le le plus Ã©levÃ© est dÃ©terminÃ© par l'ordre dans la hiÃ©rarchie dans lequel les rÃ´les sont placÃ©s, de haut en bas.
:::

**Retourne :**

- Composant `KeyPadRol` du rÃ´le de plus haute prioritÃ©
- `null` si le joueur n'a pas de rÃ´les ou si le systÃ¨me n'est pas initialisÃ©

**Exemple :**

```csharp
public UdonBehaviour securityKeypad;

public void DisplayPlayerHighestRole(VRCPlayerApi targetPlayer)
{
    KeyPadRol highestRole = securityKeypad.GetHighestRole(targetPlayer);
    
    if (highestRole != null)
    {
        Debug.Log(targetPlayer.displayName + "'s highest role is: " + highestRole.rolName);
        
        // AccÃ©der aux propriÃ©tÃ©s du rÃ´le
        string roleName = highestRole.rolName;
        Color roleColor = highestRole.color;
    }
    else
    {
        Debug.Log(targetPlayer.displayName + " has no roles");
    }
}
```

---

#### GetHighestRole()

Obtient le rÃ´le le plus Ã©levÃ© attribuÃ© au joueur local.

**Signature :**

```csharp
public KeyPadRol GetHighestRole()
```

---

#### GetHighestRole(VRCPlayerApi player)

Obtient le rÃ´le le plus Ã©levÃ© attribuÃ© Ã  un joueur spÃ©cifique.

**Signature :**

```csharp
public KeyPadRol GetHighestRole(VRCPlayerApi player)
```

**ParamÃ¨tres :**

- `player` - Le joueur dont obtenir le rÃ´le le plus Ã©levÃ©

---

### GetRoleByName

Obtient un composant `KeyPadRol` par son nom de rÃ´le.

**Signature :**

```csharp
public KeyPadRol GetRoleByName(string roleName)
```

**ParamÃ¨tres :**

- `roleName` - Le nom du rÃ´le Ã  rÃ©cupÃ©rer

**Retourne :**

- Composant `KeyPadRol` avec un nom de rÃ´le correspondant
- `null` si le rÃ´le n'est pas trouvÃ© ou si le systÃ¨me n'est pas initialisÃ©

**Comportement :**

- Retourne `null` si appelÃ© avant l'initialisation (enregistre un avertissement)
- Recherche Ã  travers tous les objets de rÃ´le pour trouver un nom correspondant
- Correspondance de nom sensible Ã  la casse

**Exemple :**

```csharp
public UdonBehaviour securityKeypad;

void GetSpecificRole()
{
    KeyPadRol adminRole = securityKeypad.GetRoleByName("admin");
    
    if (adminRole != null)
    {
        Debug.Log("Found admin role!");
        Debug.Log("Admin color: " + adminRole.color);
        
        // VÃ©rifier si le joueur local a ce rÃ´le
        bool isAdmin = securityKeypad.HasRole(adminRole);
        Debug.Log("I am admin: " + isAdmin);
    }
    else
    {
        Debug.Log("Admin role not found in keypad configuration");
    }
}
```

---

### GetRoleColor

Les mÃ©thodes `GetRoleColor` rÃ©cupÃ¨rent la couleur du rÃ´le le plus Ã©levÃ© d'un joueur.

**Retourne :**

- `Color` du rÃ´le de plus haute prioritÃ©.
- `Color.white` si le joueur n'a pas de rÃ´les, si le systÃ¨me n'est pas initialisÃ©, ou si aucune donnÃ©e de joueur n'est trouvÃ©e.

**Exemple :**

```csharp
public UdonBehaviour securityKeypad;
public MeshRenderer playerNameplate;

void UpdateMyNameplateColor()
{
    Color myRoleColor = securityKeypad.GetRoleColor();
    
    // Appliquer la couleur du rÃ´le Ã  la plaque signalÃ©tique
    playerNameplate.material.color = myRoleColor;
    
    Debug.Log("My role color is: " + myRoleColor);
}
```

---

#### GetRoleColor()

Obtient la couleur du rÃ´le le plus Ã©levÃ© attribuÃ© au joueur local.

**Signature :**

```csharp
public Color GetRoleColor()
```

---

#### GetRoleColor(VRCPlayerApi player)

Obtient la couleur du rÃ´le le plus Ã©levÃ© attribuÃ© Ã  un joueur spÃ©cifique.

**Signature :**

```csharp
public Color GetRoleColor(VRCPlayerApi player)
```

**ParamÃ¨tres :**

- `player` - Le joueur dont obtenir la couleur du rÃ´le

---

### GetPlayersWithRole(KeyPadRol role, bool implicitOnly)

Obtient une liste des joueurs qui possÃ¨dent un rÃ´le spÃ©cifique.

**Signature :**

```csharp
public VRCPlayerApi[] GetPlayersWithRole(KeyPadRol role, bool implicitOnly = false)
```

**ParamÃ¨tres :**

- `role` - Le composant KeyPadRol Ã  vÃ©rifier
- `implicitOnly` - Si `true`, ne considÃ¨re que les tags de rÃ´le implicites (par dÃ©faut : `false`)

**Retourne :**

- Tableau de joueurs `VRCPlayerApi` qui possÃ¨dent le rÃ´le spÃ©cifiÃ©
- Tableau vide si aucun joueur n'a ce rÃ´le ou si le systÃ¨me n'est pas initialisÃ©
